import {StyleSheet} from 'react-native';

export const styles = StyleSheet.create({
  container: {
    flexDirection: 'row',
    // paddingVertical: 10,
    borderColor: '#4b4b4b',
    borderWidth: 1,
    alignItems: 'center',
    borderRadius: 7,
  },
  input: {
    width: '85%',
    fontSize: 15,
    paddingLeft: 15,
  },
  arrowIcon: {
    position: 'absolute',
    right: 16,
  },
});
